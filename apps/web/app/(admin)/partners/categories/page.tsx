import { DirectoryPage } from "@/modules/partners/ui/directory-page";
export const metadata = { title: "Categorias de parceiros" };
export default function Page() {
  return <DirectoryPage area="categories" />;
}
